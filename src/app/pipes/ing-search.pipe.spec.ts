import { IngSearchPipe } from './ing-search.pipe';

describe('IngSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new IngSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
